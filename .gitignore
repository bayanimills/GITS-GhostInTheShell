# Temporary files
.DS_Store
Thumbs.db
*.tmp
*.temp
*.swp
*~

# Logs
*.log
logs/

# Local backups (tarballs) are kept in repository, but we ignore any stray files
backup-*.tar.gz

# System files
.env
.secret

# Personal config (use .example files as templates)
config/openclaw.json
config/agents.list
config/jobs.json

# Credential backups (instance-specific)
credentials/*.tar.gz

# IDE
.vscode/
.idea/